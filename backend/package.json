{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "This is the server side of project",
  "scripts": {
    "dev":"node --env-file=.env --import=tsx src/index.ts",
    "watch": "node --env-file=.env --import=tsx --watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js --env-file=.env"
  },
  "keywords": [],
  "author": "SteGG200",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.4"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "tsx": "^4.15.8",
    "typescript": "^5.5.2"
  }
}
